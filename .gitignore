*.iml
.gradle
/local.properties
/.idea
.DS_Store
# Ignore every module's build output, not just the root. This is a multi-module
# project (app, core/*, feature/*, network, toolchain, build-logic/convention),
# so a leading-slash "/build" would leave the per-module build/ dirs untracked.
build/
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/
out/
release/
.navigation/
*.hprof
*.jks
*.keystore
!gradle/wrapper/gradle-wrapper.jar
build-logic/convention/build/
